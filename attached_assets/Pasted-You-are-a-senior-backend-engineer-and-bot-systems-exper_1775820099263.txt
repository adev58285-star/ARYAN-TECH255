You are a senior backend engineer and bot systems expert.

I have a bot that either:
- fails to connect properly, OR
- connects but does NOT process incoming messages

Your task is to fully diagnose and fix both connectivity AND message handling.

Context:
- Bot type: [e.g., Telegram / Discord / WhatsApp / custom]
- Language/framework: [e.g., Python, Node.js]
- Hosting: [local / VPS / Docker / cloud]
- Integration method: [webhook / polling / gateway]
- Current issue: [describe briefly]
- Logs/errors: [paste full logs]
- Relevant code: [paste connection + message handler code]

What I need:
1. Identify why the bot is not receiving OR processing messages.
2. Check BOTH:
   A. Connection layer
   B. Message handling pipeline

Debug systematically across:

CONNECTION CHECKS:
- API/Gateway reachable
- सही endpoint URLs
- Token/API key validity
- WebSocket status (if used)
- Webhook registration (if used)
- Polling loop running (if used)

MESSAGE FLOW CHECKS:
- Is the bot actually receiving events?
- Are handlers/listeners registered correctly?
- Are message filters too strict?
- Is there an event loop/blocking issue?
- Are messages being dropped or ignored?

COMMON ROOT CAUSES TO CHECK:
- Bot connected but no handler attached
- Webhook set but server not publicly reachable
- Polling not started or crashing silently
- Missing permissions (e.g., bot not allowed to read messages)
- Wrong event type subscription
- Async code not awaited properly
- Exceptions stopping the handler silently

REQUIRED OUTPUT:
1. Most likely root cause
2. Step-by-step debugging plan
3. Exact fixes (code-level where possible)
4. Minimal working example showing:
   - successful connection
   - receiving a message
   - logging or replying to it

5. Add logging improvements to confirm:
   - connection established
   - message received
   - message processed

Important:
- Be precise and actionable
- Prefer concrete fixes over theory
- Assume the bot SHOULD echo or log messages once fixed